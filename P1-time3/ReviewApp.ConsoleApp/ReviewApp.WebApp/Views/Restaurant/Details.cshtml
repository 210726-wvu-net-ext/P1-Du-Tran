@model IEnumerable<ReviewApp.Domain.Review>

@{
    ViewData["Title"] = "Review";
}

<h1>Restaurant Review</h1>

<table class="table">
    <thead>
        <tr>

            <th>
                @Html.DisplayNameFor(model => model.Comment)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rating)
            </th>
   
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.Comment)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rating)
            </td>
            
            <td>
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                @if (User.Identity.IsAuthenticated)
                    {@Html.ActionLink("Edit", "Edit", new { id = item.Id })  @Html.DisplayName(" | ")
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })}
            </td>
        </tr>
        }
    </tbody>

</table>

@*<div>
        <form asp-action="LeaveReviews">
            <div class="form-group">
                <label asp-for="Comment" class="control-label" style="font-weight:bold;">Leave your comment</label>
                <input asp-for="Comment" class="form-control" />
                <span asp-validation-for="Comment" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Rating" class="control-label" style="font-weight:bold;">Rating</label>
                <input asp-for="Rating" class="form-control" />
                <span asp-validation-for="Rating" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Submit" class="btn btn-primary" />
            </div>
        </form>
    </div>*@
<div>
    <a asp-action="LeaveReviews">Leave Your Review</a> <br />
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |

    <a asp-action="Index">Back to List</a>
</div>
